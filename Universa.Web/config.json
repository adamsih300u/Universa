{
    "server": {
        "port": 8080,
        "host": "0.0.0.0"
    },
    "database": {
        "host": "${DB_HOST}",
        "port": ${DB_PORT},
        "user": "${DB_USER}",
        "password": "${DB_PASSWORD}",
        "name": "${DB_NAME}",
        "sslmode": "${DB_SSLMODE}"
    },
    "sync": {
        "username": "${SYNC_USERNAME}",
        "password": "${SYNC_PASSWORD}"
    },
    "jwt": {
        "secret": "${JWT_SECRET}",
        "expiry": "24h"
    },
    "storage": {
        "path": "${FILE_STORAGE_PATH}"
    }
} 